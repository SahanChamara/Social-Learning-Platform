package com.sociallearning.enums;

/**
 * Target type for likes - defines what entity type a like belongs to
 */
public enum LikeableType {
    COURSE,      // Like on a course
    LESSON,      // Like on a lesson
    COMMENT      // Like on a comment
}
